import { useStyles } from "./styes";

const CreateMemory = () => {
  const { classes } = useStyles();
  return <section className={classes.section}>CreateMemory</section>;
};

export default CreateMemory;
